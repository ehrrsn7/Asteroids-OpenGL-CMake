

Build

```bash
cmake --build build && ./build/Asteroids
```

Generated by the Glad Make generator service

<https://glad.dav1d.de/>

Explore these for Cross Platform:

### Windows

```powershell
# Install CMake to your user profile
winget install --id Kitware.CMake --scope user -e

# Install MinGW (GCC/Make) to your user profile
winget install --id BrechtSanders.WinLibs.POSIX.UCRT --scope user -e
```

**First time install**
```powershell
# 1. Configure CMake to explicitly use MinGW (Only needs to be run once)
cmake -B build -G "MinGW Makefiles"
# set powershell execution policy
```

**Windows equivalent of running**
```powershell
cmake --build build && .\build\Asteroids.exe
```
(For Powershell v6 and below)
```powershell
# 2. Build and run (The exact replacement for your Linux command)
cmake --build build
if (-not $?) { exit } 
.\build\Asteroids.exe
```
